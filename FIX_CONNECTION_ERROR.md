# 🔧 OCR 连接错误修复指南

## ✅ 已完成的修复

我们已经修改了代码来防止 `ECONNRESET` 错误：

1. **前端 (vite.config.ts)**
   - 增加代理超时：**5分钟** (300000ms)
   - 防止前端主动断开连接

2. **后端 (backend/main.py)**
   - 增加文件大小限制：**50MB**
   - 增加处理超时保护：**4分钟**
   - 增加全局错误捕获：防止崩溃

---

## 🚀 必须执行的步骤

修改配置后，**必须重启服务才能生效**：

### 1. 重启前端 (关键！)
Vite 的配置修改 (`vite.config.ts`) 需要重启才能加载。
- 在运行 `npm run dev` 的终端，按 `Ctrl+C` 停止
- 再次运行 `npm run dev`

### 2. 重启后端
- 在运行 `uvicorn` 的终端，按 `Ctrl+C` 停止
- 再次运行 `uvicorn main:app --reload`

---

## ❓ 如果问题依然存在

如果重启后仍然报错，请检查：

1. **文件大小**：是否超过 50MB？
2. **文件类型**：是否是加密的 PDF？
3. **网络环境**：是否使用了 VPN 或代理？

---

> *"The phantom thieves strike again!"*
