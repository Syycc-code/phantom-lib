# 🎩 Phantom Library - 安装指南

## ✅ 已修复的问题

1. **onnxruntime DLL 错误** - 已锁定到稳定版本 1.19.2
2. **RAG 功能依赖** - 已改为可选加载，即使失败也不影响主功能
3. **依赖版本锁定** - requirements.txt 已更新

---

## 🚀 快速启动

### 方法 1：一键启动（推荐）

双击 `start_phantom.bat` 即可启动前后端

### 方法 2：手动启动

#### 后端启动
```bash
cd backend
pip install -r requirements.txt
uvicorn main:app --reload --host 127.0.0.1 --port 8000
```

#### 前端启动（新开终端）
```bash
npm install
npm run dev
```

访问：http://localhost:5173

---

## ⚠️ 可选功能：RAG 聊天（需要额外步骤）

**当前状态：RAG 功能已禁用（PyTorch DLL 问题）**

RAG 聊天功能需要 PyTorch，而 PyTorch 在 Windows 上需要：

### 解决方案 A：安装 Visual C++ Redistributable
1. 下载并安装：https://aka.ms/vs/17/release/vc_redist.x64.exe
2. 重启电脑
3. 重新启动后端

### 解决方案 B：不使用 RAG 功能（当前状态）
- ✅ PDF 阅读 / OCR 识别
- ✅ Mind Hack（文本分析）
- ✅ 文档融合
- ✅ 自动标签
- ❌ RAG 聊天（暂时禁用）

---

## 📦 当前可用的核心功能

| 功能 | 状态 | 说明 |
|------|------|------|
| PDF 阅读器 | ✅ | 完全可用 |
| OCR 文字识别 | ✅ | RapidOCR 工作正常 |
| Mind Hack | ✅ | DeepSeek AI 分析 |
| 自动标签 | ✅ | 智能分类 |
| 文档融合 | ✅ | 跨文档对比 |
| RAG 聊天 | ⚠️ | 需要安装 VC++ Redistributable |

---

## 🔧 环境要求

- **Python**: 3.12.4
- **Node.js**: 推荐 v18+
- **操作系统**: Windows 10/11

---

## 🐛 常见问题

### Q: 后端启动报 DLL 错误
A: 这是正常的，RAG 功能会自动禁用。其他功能不受影响。

### Q: 如何启用 RAG 聊天功能？
A: 安装 Visual C++ Redistributable（见上方链接）

### Q: DeepSeek API 配置
A: 在项目根目录创建 `.env` 文件：
```
DEEPSEEK_API_KEY=your_key_here
```

---

## 📝 版本信息

- **onnxruntime**: 1.19.2（已锁定）
- **chromadb**: 1.4.1
- **sentence-transformers**: 5.2.0
- **React**: 19.2.0
- **FastAPI**: 最新版

---

> *"Wake up, Get up, Get out there."*
